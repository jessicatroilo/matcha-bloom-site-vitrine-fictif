<!DOCTYPE html>
<html lang="fr">
    <head>
    <!-- META -->
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="Entrez dans l'univers de notre Salon Matcha Bloom, spécialisé dans le matcha. Venez dans notre salon déguster nos spécialités!"> 

        <title>{% block title %}Bienvenue chez Matcha Bloom !{% endblock %}</title>

        <!-- FAVICON -->
        <link rel="shortcut icon" href="{{ asset('images/salon-icon.svg') }}" type="image/x-icon" alt="Logo de l'entreprise">

        <!-- IMG -->
        
        <!-- FONT AWESOME, Icons -->
        <script src="https://kit.fontawesome.com/275e06ef04.js" crossorigin="anonymous"></script>

        <!-- CSS, Tailwindcss -->
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        <!-- JS, Encore -->
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="flex flex-col min-h-screen relative">
            <!-- HEADER, NAV -->
            <div class="flex h-screen">
            <!-- BARRE DE NAVIGATION VERTICALE -->
            <nav class="w-64 bg-gray-600 text-white p-6 flex flex-col gap-4">
                <h1 class="text-xl font-bold mb-4">Backoffice</h1>
                <a href="{{ path('app_back_admin') }}" class="hover:bg-gray-700 p-2 font-bold rounded">Tableau de bord</a>
                <a href="{{ path('app_back_favorite_product_list') }}" class="hover:bg-gray-700 p-2 rounded"> Gestion de la Page d'accueil</a>
                <a href="{{ path('back_menu_upload') }}" class="hover:bg-gray-700 p-2 rounded">Gestion du Menu</a>
                <a href="#" class="hover:bg-gray-700 p-2 rounded">Mon compte</a>
                <a href="#" class="text-red-400 hover:bg-gray-700 p-2 font-bold rounded">Déconnexion</a>
            </nav>

            <!-- CONTENU PRINCIPAL -->
            <div class="flex-1 flex flex-col">
                <!-- HEADER -->
                <header class="bg-gray-200 px-6 py-4 flex justify-between items-center">
                    <p class="text-lg font-semibold">Matcha Bloom</p>
                    <a href="#" class="text-matcha-accent font-bold">Mon compte</a>
                </header>

                <!-- MAIN -->
                <main class="flex-1 overflow-auto p-6 bg-gray-100">
                    {% block body %}{% endblock %}
                </main>

                <!-- FOOTER -->
                <footer class="bg-gray-100 text-center text-sm py-4">
                    <p class="font-bold text-matcha-accent">&copy; 2025 - Matcha Bloom - 
                        Site par Jessica Troilo 
                        <a href="https://www.linkedin.com/in/jessica-troilo-dev/" target="_blank"><i class="fa-brands fa-linkedin-in"></i></a>
                        <a href="https://github.com/jessicatroilo" target="_blank"><i class="fa-brands fa-github"></i></a>
                    </p>
                </footer>
            </div>
        </div>
    </body>
</html>
