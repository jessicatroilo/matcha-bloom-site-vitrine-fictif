{% extends 'base.html.twig' %}

{% block title %}Notre gallerie - Photos de Matcha Bloom{% endblock %}

{% block body %}
<!--Section Gallery -->
<div class="flex flex-col w-full max-w-full min-h-screen">
    <section class="flex justify-center bg-all-page bg-footer-text lg:bg-contain bg-fill bg-right-top bg-w-full relative">
        <div class="flex lg:flex-row flex-col justify-center items-center flex-wrap gap-x-96 my-24 ">
            <h2 class="text-text font-bold lg:text-4xl text-2xl lg:me-56">Gallerie</h2>
            <img src="{{ asset('images/gallery-icon.webp') }}" 
                alt="icône d'une main avec un smarphone qui fait une photo" 
                aria-hidden="true"
                class="w-48 h-auto object-fill">
        </div>
    </section>
    <section>
    <h3>En cours de construction...En attendant buvez un petit matcha et détendez-vous</h3>
    </section>
</div>
{% endblock %}
